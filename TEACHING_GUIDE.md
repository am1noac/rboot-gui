# Rboot GUI 示教功能使用指南

## 📋 示教功能完整流程

### 前置条件
1. ✅ 设备已上电
2. ✅ USB线连接到COM7
3. ✅ 程序已启动并连接设备
4. ✅ CAN总线已连接

---

## 🎯 示教模式操作步骤

### 步骤1: 启动程序并连接设备

```bash
cd D:\Works\dummy2-master\dummy2-gui\rboot-gui
python main.py
```

在Web界面(http://127.0.0.1:8081)中：

1. **选择连接模式**: 确保是"串口 (USB)"
2. **点击"连接设备"** → 等待显示 "✓ 串口已连接!"
3. **点击"连接CAN总线"** → 等待显示 "CAN BUS: Enabled" (绿色)

---

### 步骤2: 切换到示教模式

在界面顶部的模式切换器中，选择 **"Teaching"** 模式

此时界面会显示：
- 示教点列表
- Add（添加点位）按钮
- Delete（删除点位）按钮
- Repeat（重复执行）按钮
- Save（保存）按钮
- Open（打开）按钮

---

### 步骤3: 设置电机为IDLE状态（重要！）

**在开始手动移动之前，必须将所有电机设为IDLE状态，否则无法拖动！**

有两种方法：

**方法A（推荐）：一键设置所有电机**
- 点击界面上方的 **"× 圆形按钮"**（Enable all joints to idle mode）
- 这会将6个电机同时设为IDLE状态

**方法B：单独设置**
- 切换到 "Status" 模式
- 对每个电机卡片，点击 **"× 圆形按钮"**（Enable idle mode）

**验证IDLE状态：**
- 切换到"Status"模式
- 检查每个电机的 Status 字段应该显示 **1** (IDLE)
- 此时机械臂应该可以手动拖动了

---

### 步骤4: 手动移动机械臂并记录位置

1. **手动拖动机械臂到第一个位置**
   - 轻轻拖动机械臂关节
   - 移动到您想要的姿态

2. **记录当前位置**
   - 确保在 "Teaching" 模式
   - 点击 **"Add"** 按钮
   - 当前6个关节的位置会被记录为一个示教点

3. **继续添加更多位置**
   - 重复步骤1和2
   - 每次Add都会在列表中添加一个新的步骤

示教点列表会显示：
```
Step 1
  Joint1: 0.000
  Joint2: 15.320
  Joint3: -10.500
  ...

Step 2
  Joint1: 5.240
  Joint2: 20.100
  ...
```

---

### 步骤5: 设置夹爪状态（可选）

在添加每个示教点时，可以设置夹爪状态：

1. **切换到 "Controls" 模式**
2. 在 "Gripper Control" 卡片中：
   - **Input torque**: 设置夹爪力矩（0-1.0）
   - 点击 **"← 按钮"**: 打开夹爪
   - 点击 **"→ 按钮"**: 关闭夹爪

3. **切回 "Teaching" 模式**
4. 点击 **"Add"** 记录当前位置和夹爪状态

---

### 步骤6: 回放示教动作

**准备回放：**

1. **设置电机为闭环控制模式**
   - 点击界面上方的 **"↻ 圆形按钮"**（Enable all joints to close loop mode）
   - 或在Status模式下，为每个电机点击↻按钮

2. **设置回放参数**
   - **Step delays(s)**: 设置每个步骤之间的延迟时间（如1.5秒）
   - **Repeat times**: 设置重复次数（如1次）

3. **开始回放**
   - 点击 **"Repeat"** 按钮
   - 机械臂会按照记录的顺序执行所有动作

**注意事项：**
- ⚠️ 回放前必须将电机设为闭环控制模式（Status=8）
- ⚠️ 确保机械臂运动范围内没有障碍物
- ⚠️ 首次回放建议将延迟时间设长一些（如3秒）

---

### 步骤7: 保存和加载示教文件

**保存示教序列：**
1. 点击 **"Save"** 按钮
2. 选择保存位置和文件名（JSON格式）
3. 示教点位会保存到文件

**加载示教序列：**
1. 点击 **"Open"** 按钮
2. 选择之前保存的JSON文件
3. 示教点位会加载到列表中

示教文件格式示例：
```json
[
  {
    "J1": 0.0,
    "J2": 15.32,
    "J3": -10.5,
    "J4": 0.0,
    "J5": 5.18,
    "J6": 0.0,
    "Delay": 1.5,
    "Gripper": 0,
    "Torque": 0.5
  },
  {
    "J1": 5.24,
    ...
  }
]
```

---

## 🔧 常见问题

### Q1: 机械臂无法手动拖动
**原因**: 电机处于闭环控制模式（Status=8）

**解决**:
1. 切换到Status模式
2. 对每个电机点击"× 圆形按钮"设为IDLE
3. 确认Status字段变为1

### Q2: 点击Add后没有反应
**原因**: CAN总线未连接或未收到电机位置数据

**解决**:
1. 检查"CAN BUS: Enabled"是否显示为绿色
2. 切换到Status模式，查看各电机的position是否在实时更新
3. 如果没有更新，说明未收到设备响应，检查设备连接

### Q3: 回放时机械臂不动
**原因**: 电机未设为闭环控制模式

**解决**:
1. 点击界面上方的"↻"按钮（Enable all joints to close loop mode）
2. 等待2秒让电机进入闭环模式
3. 再点击Repeat按钮

### Q4: 回放动作与示教不一致
**原因**: 减速比配置错误或位置记录时机械臂不在IDLE状态

**解决**:
1. 确保示教时电机都在IDLE状态
2. 检查motors_cfg中的reduction值是否正确：
   - M1-M3, M5-M6: reduction=50
   - M4: reduction=30

---

## 📊 界面模式说明

### Status 模式（监控模式）
- 显示所有电机的实时状态
- 位置、速度、电流、电压等
- 可单独控制每个电机的状态

### Controls 模式（控制模式）
- 手动控制单个关节
- 手动控制整个机械臂姿态
- 夹爪控制

### Teaching 模式（示教模式）
- 记录示教点位
- 管理示教序列
- 回放示教动作
- 保存/加载示教文件

---

## ⚡ 快速操作流程总结

```
1. 连接设备 → 连接CAN总线
                    ↓
2. 切换到Teaching模式
                    ↓
3. 点击"× 圆形按钮"设置所有电机为IDLE
                    ↓
4. 手动拖动机械臂到位置1 → 点击Add
                    ↓
5. 手动拖动机械臂到位置2 → 点击Add
                    ↓
6. ... 重复添加更多位置
                    ↓
7. 点击"↻ 圆形按钮"设置所有电机为闭环控制
                    ↓
8. 设置Step delays和Repeat times
                    ↓
9. 点击Repeat按钮开始回放
                    ↓
10. (可选) 点击Save保存示教文件
```

---

## ⚠️ 安全提示

1. **首次使用建议**:
   - 先在单个关节上测试
   - 确认方向和速度正确后再使用整臂

2. **运动范围检查**:
   - 确保机械臂工作空间内无障碍物
   - 注意关节限位（J1: ±175°, J2: -115°~75°等）

3. **紧急停止**:
   - 如需紧急停止，点击"断开CAN总线"
   - 或直接关闭程序
   - 或切断电源

4. **示教质量**:
   - 移动要平稳，避免突然用力
   - 每个示教点之间留有足够时间间隔
   - 首次回放时增大延迟时间

---

## 💾 示教文件管理

**推荐的文件命名规范：**
```
pick_and_place_v1.json      # 抓取放置动作
welding_sequence_v2.json    # 焊接序列
assembly_step1_v3.json      # 装配步骤1
```

**备份建议：**
- 定期备份重要的示教文件
- 每次修改后保存新版本
- 在真实任务前先在安全环境测试

---

如有其他问题，请参考 TROUBLESHOOTING.md 或查看主程序控制台的调试信息。
